const corsOptions = {
    //origin: 'http://tudominio.com', // Reemplaza con el dominio que deseas permitir
    methods: ['GET', 'POST', 'PUT'],
    //credentials: true, // Si necesitas enviar cookies
};

export default corsOptions